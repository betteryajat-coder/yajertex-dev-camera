name: Build APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.22.0"
          channel: stable
          cache: true

      - name: Flutter doctor
        run: flutter doctor -v

      # Scaffold the Android project skeleton (gradle wrapper, MainActivity,
      # build.gradle, etc.). Existing Dart source is left untouched.
      - name: Scaffold Android project
        run: |
          flutter create --project-name yajertex_dev_camera \
                         --org com.yajertex \
                         --platforms=android \
                         .

      # Overlay our custom AndroidManifest.xml (adds camera + location
      # permissions that flutter create doesn't include by default).
      - name: Apply custom AndroidManifest
        run: |
          cp android-patches/AndroidManifest.xml \
             android/app/src/main/AndroidManifest.xml

      # The scaffolded project pins Kotlin too low for newer plugins
      # (e.g. camera). Bump to 1.9.22.
      - name: Bump Kotlin plugin version
        run: |
          if [ -f android/settings.gradle ]; then
            sed -i 's/id "org.jetbrains.kotlin.android" version "[^"]*"/id "org.jetbrains.kotlin.android" version "1.9.22"/' android/settings.gradle || true
          fi
          if [ -f android/build.gradle ]; then
            sed -i "s/ext.kotlin_version = '[^']*'/ext.kotlin_version = '1.9.22'/" android/build.gradle || true
          fi
          echo '--- settings.gradle ---'
          grep -i kotlin android/settings.gradle || true
          echo '--- build.gradle ---'
          grep -i kotlin android/build.gradle || true

      - name: Pub get
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: yajertex-dev-camera-release
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
